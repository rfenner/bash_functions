# bash_function

This repository contains a series of bash function i utilize in writing shell scripts.

Contains:
* Functions to ask questions and return an asnwer
* Functions to deal with aws secrets, ssm and getting the region
* Checking if a file has been modified
* Function if local stack is running
* Functions for github related to ssh keys and known hosts
* Functions to working with the /etc/hosts file

Se each function for documentation on it's use